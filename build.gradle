plugins {
    id 'java'
    id 'idea'
    id 'jacoco'
}

defaultTasks 'idea', 'build'

repositories {
    mavenCentral()
}

dependencies {
    compile     'com.google.guava:guava:+'
    testCompile 'junit:junit:+',
                'org.mockito:mockito-all:+'
}
