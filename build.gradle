apply plugin: 'java'
apply plugin: 'idea'

defaultTasks 'build', 'idea'

repositories {
    mavenCentral()
}

dependencies {
    testCompile 'junit:junit:+',
                'org.mockito:mockito-all:+'
}

task wrapper(type: Wrapper) {
  gradleVersion = '1.3'
}

